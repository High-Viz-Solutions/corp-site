{
    "scripts": {
        "build": "bundle install && npm install && bundle exec jekyll build"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.2",
        "postcss": "^8.4.6",
        "postcss-cli": "^9.1.0",
        "tailwindcss": "^3.0.23"
    }
}
